<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--Change the application background color-->
        <item name="android:background">#D1C4E9</item>
        <!--Style the button color so that it has the color accent as other views-->
        <item name="colorButtonNormal">@color/colorAccent</item>
    </style>

    <!--Create a style that works for TextViews and all views that follow the same styling attributes-->
    <style name="TextViewStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">16dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!--Create a style that works for Radio Buttons and other views that follow the same styling attributes-->
    <style name="RadioStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:layout_marginBottom">16dp</item>
    </style>

</resources>
